{
  "name": "html-resume",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite --open",
    "build": "vite build",
    "preview": "vite preview",
    "pdf": "npm run build && node scripts/export-pdf.mjs"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "sass": "^1.104.1",
    "vite": "^8.3.0"
  }
}
